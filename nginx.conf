server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    gzip on;
    gzip_types text/css text/html application/javascript image/svg+xml application/xml;

    error_page 404 /404.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
